
@inject IStringLocalizer<Resources> localizer

<div class="radio-group">
    @foreach ((FlagDifficulty difficulty, string name) in GetDifficulties())
    {
        <div class="radio">
            <label for="@GetRadioId(name)">@localizer[name]</label>
            <input id="@GetRadioId(name)"
                   name="@GroupName"
                   type="radio"
                   disabled="@Disabled"
                   checked="@(Value == difficulty)"
                   @onchange="async () => await ValueChanged.InvokeAsync(difficulty)" />
        </div>
    }
</div>
